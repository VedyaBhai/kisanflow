import React from "react";
import ReactDOM from "react-dom/client";
import KisanflowApp from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <KisanflowApp />
  </React.StrictMode>
);
