# KISANFLOW Phase 2

This branch prepares the prototype for a real Supabase-backed implementation without redesigning the existing UI.

Next steps:
1. Create Supabase project.
2. Run `supabase/schema.sql`.
3. Add `.env.local` from `.env.example`.
4. Run `npm install` and `npm run build`.
5. Wire current UI pages to the service layer.
6. Import government market/crop/weather data.
7. Implement eligible-farmer matching.
8. Implement demand forecasting.
9. Implement WhatsApp AI and Telugu voice.
